var API_ROOT = '/api';

var constants = {
  api: {
    root: API_ROOT,

    slides: API_ROOT + "/slides",
    timetables: API_ROOT + "/timetables",
    duties: API_ROOT + "/duties"
  }
};

module.exports = constants;
